import { addJob, getJobs } from '../repositories/jobsRepository';
import { getCandidates, getMyResume, saveMyResume } from '../repositories/resumeRepository';

export { getJobs, addJob, getCandidates, getMyResume, saveMyResume };
